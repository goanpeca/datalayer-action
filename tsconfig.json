{
  "//": "Root type-check config. esbuild builds the action bundles; tsc only type-checks (never emits). `npm run type-check` runs tsc over all workspace source.",
  "extends": "./tsconfig.base.json",
  "include": ["packages/*/src/**/*.ts", "actions/*/src/**/*.ts"]
}
