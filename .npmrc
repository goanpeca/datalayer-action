# Pin exact dependency versions on install (reproducible builds across the monorepo).
save-exact=true

# Refuse to install if the local toolchain violates the `engines` field in package.json.
engine-strict=true

# Keep the lockfile authoritative.
package-lock=true
